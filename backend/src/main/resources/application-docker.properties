# H2 Database Configuration for Docker (File-based)
spring.datasource.url=jdbc:h2:file:/app/data/formdb;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password

# JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Application Settings
app.jwt.secret=${JWT_SECRET:04ca891008010300020d050f0101080c0d050f0101080c0d050f0101080c0d050f0101080c0d050f0101080c}
app.upload.dir=/app/uploads
app.ocr.engine-url=${OCR_ENGINE_URL:http://ocr-engine:8001}
